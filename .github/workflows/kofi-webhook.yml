name: Ko-fi Webhook (manual)

on:
  workflow_dispatch:

jobs:
  describe-setup:
    runs-on: ubuntu-latest
    steps:
      - name: Show manual setup notes
        run: |
          echo "This workflow is intentionally manual-only."
          echo "To use Ko-fi automation, configure a repository_dispatch secret and backend separately."
